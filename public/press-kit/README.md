# HELIOBANE: press kit

HELIOBANE is an Amiga-style vertical shoot 'em up by **Gand**. Everything in
this kit may be used for coverage, previews, reviews and storefronts. Credit
line: **"HELIOBANE, a game by Gand (https://heliobane.gand.games)"**.
Press contact: contact@gand.tr.

The press page is `doc.html` (English and Japanese in one file). The whole kit
is also one archive: `heliobane-press-kit_2026-09-25.zip`.

## Folders

| Folder | What |
|---|---|
| `doc.html` | the press page: fact sheet, features, trailer, history, screenshots, downloads, contact (EN / 日本語) |
| `factsheet.md` | the same facts as plain text |
| `key-art/` | `key_art.png` 1920×1080, `key_art_4k.png` 3840×2160, `key_art_no_logo.png`, `key_art_vertical.png` 1200×1800, `launch_painting_no_logo.png` (the intro's launch scene) |
| `logo/` | transparent PNG at ×1 (the in-game 242×64), ×4, ×8; on dark; on stars |
| `screenshots/` | 26 in-game captures at 1280×1024 (×4); `native/` has the 320×256 originals. `ja_*` and `tr_*` are the Japanese and Turkish versions |
| `steam-screenshots/` | the same captures at 1920×1080, as on the Steam page; `demo/` for the demo |
| `steam/` | Steam capsules, library images, icons and event images; `steam/demo/` for the demo. Sizes in `steam/ASSETS.md` |
| `icons/` | app icon 32–512 px, `.ico`, 184 px community icon |
| `social/` | `og_1200x630.png` link card, `square_1080.png` |
| `video/` | the trailer (83 s, 1920×1080, 60 fps, H.264), English captions, poster, thumbnail |
| `fonts/` | Public Pixel (CC0), the game's pixel font, used by `doc.html` |

## About the images

Screenshots are real in-game frames: the game draws at 320×256 and they are
scaled by exactly 4 (1280×1024). The Steam versions put that picture on black
at 1920×1080, which is how the game looks full-screen on a 1080p display.

The key art and capsules are built from the game's own logo, ship sprites,
bitmap fonts and intro scene, over backdrops fitted to the same 32-colour
palette. AI was used to generate some of the game's assets (artwork, voice
lines, sound effects, in-game text and translations), and some of the store
and marketing art is AI-assisted; artwork was edited and fitted to the
palette and style.

All pixel art is scaled by whole numbers with nearest-neighbour. If you
resize it, please do the same, or start from the ×1 files in `logo/` and
`screenshots/native/`.
